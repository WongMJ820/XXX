name: PocketCFO CI/CD Pipeline

on:
  push:
    branches: [ "main", "dev" ]
  pull_request:
    branches: [ "main" ]

jobs:
  quality-assurance:
    name: Code Quality & Testing
    runs-on: ubuntu-latest
    
    defaults:
      run:
        working-directory: ./pocketcfo

    steps:
      - name: Checkout Code Repository
        uses: actions/checkout@v4
      
      - name: Setup Node.js Environment
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: pocketcfo/package-lock.json
          
      - name: Install Dependencies
        run: npm ci

      - name: Code Linting
        run: npm run lint
        
      - name: Run Unit Tests
        run: npm test --if-present

  build:
    name: Production Build Verification
    runs-on: ubuntu-latest
    needs: quality-assurance
    
    defaults:
      run:
        working-directory: ./pocketcfo

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: pocketcfo/package-lock.json
          
      - name: Install dependencies
        run: npm ci
        
      - name: Build Next.js Application
        run: npm run build
        env:
          NEXT_TELEMETRY_DISABLED: 1
